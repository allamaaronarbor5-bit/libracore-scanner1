{
  "name": "libracore-scanner-server",
  "version": "1.0.0",
  "description": "WebSocket relay server for LibraCore real-time QR scanning",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "ws": "^8.17.0"
  },
  "engines": {
    "node": ">=16"
  }
}
